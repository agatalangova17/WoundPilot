import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Analytics Coming Soon")
                    .font(.title3)
                    .foregroundColor(.gray)
            }
            .navigationTitle("Analytics")
        }
    }
}
